class Messages {
    successMessage = {
        addToCart: "Product added to cart."
    }

    errorMessage = {
        addToCart: "Failed to add to cart."
    }
}

const messageConfig = new Messages();

export default messageConfig